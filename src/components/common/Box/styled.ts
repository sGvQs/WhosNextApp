import styled from 'styled-components';
import { Html } from '@react-three/drei';

export const StyledText = styled(Html)`
  white-space: nowrap;
  font-size: 2rem;
  color: white;
  margin-top: 15vh;
  user-select: none;
  font-weight: bold;
`;
